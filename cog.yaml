build:
  gpu: true

  system_packages:
    - "ffmpeg"
    - "libmagic1"
    - "cudnn9-cuda-12"
    # - "libgl1-mesa-glx"
    # - "libglib2.0-0"

  python_version: "3.10"
  python_requirements: requirements.txt

  run:
    - "echo env is ready!"

predict: "cog_predictor.py:Predictor"